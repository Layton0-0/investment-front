# 백엔드 API Base URL (미설정 시 http://localhost:8083 사용)
# 기본 포트 8083. 8084는 Agent/테스트 전용이므로 일반 개발 시 설정하지 말 것.
# VITE_API_BASE_URL=http://localhost:8083

# 프로덕션(도메인 사용 시): EXAMPLE_DOMAIN을 실제 도메인으로 치환. 값은 .env에만 설정하고 저장소에 커밋하지 말 것.
# VITE_API_BASE_URL=https://api.EXAMPLE_DOMAIN
