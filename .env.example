# 백엔드 API Base URL. 미설정 시 자동: 로컬(localhost/127.0.0.1) → http://localhost:8080, 배포 도메인 → 현재 origin (한 빌드로 로컬·배포 공용).
# VITE_API_BASE_URL=http://localhost:8080

# API가 현재 도메인과 다를 때만 지정 (예: api.별도도메인). 값은 .env에만 두고 저장소에 커밋하지 말 것.
# VITE_API_BASE_URL=https://api.EXAMPLE_DOMAIN
