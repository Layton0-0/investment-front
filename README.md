# investment-front (React)

## Run

```bash
npm install
npm run dev
```

## Backend API base URL

Vite 환경변수 `VITE_API_BASE_URL`로 백엔드 주소를 지정합니다.

- 예: `http://localhost:8083` (IntelliJ 실행)

PowerShell 예시:

```powershell
$env:VITE_API_BASE_URL="http://localhost:8083"
npm run dev
```

